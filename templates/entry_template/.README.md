each entry should have:

1. a README markdown file
2. a tags YAML file, named `metadata.yaml`
3. some script for making the data/information, preferably python or notebook   
4. the information stored in some extension like json or such
5. the long description stored in some markdown or text files
6. the visualizer stored as graphic or audio files

`.README.md` and `metadata.yaml` MUST NOT be renamedi

IF the scripts, docs, and assets are getting cluttered, it COULD be grouped by folders but the folders MUST be named with `scripts`, `docs`, and `assets`. 

assets are stored per entry, thus is not unified for the whole project.

`.README.md` file contains:
1. Directory Map of the entry